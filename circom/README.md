# Circom Circuits

This directory contains all Circom circuits used in this project.  
The `circuits/` folder includes raw circuits, some of which are from other libraries, such as circomlib.  

The `helper_scripts/` folder provides utility scripts for:
- Compiling circuits to R1CS
- Generating input skeletons
- Computing witnesses
- Patching commitment circuits with new keys